# Frontend env — see REBUILD_SPEC.md decision #10.
# Only NEXT_PUBLIC_* and publishable (non-secret) keys belong here.
# No DATABASE_URL, no *_SECRET_KEY, no OPENAI_API_KEY — those live in /backend only.

NEXT_PUBLIC_API_URL=https://api.techbrief.voltixio.com
NEXT_PUBLIC_APP_URL=https://techbrief.voltixio.com
