# Copy to .env.production on the VPS, fill in real values, and run
# docker compose --env-file .env.production -f docker-compose.prod.yml up -d
# Must match the credentials used in backend/.env.production's DATABASE_URL.

POSTGRES_USER=
POSTGRES_PASSWORD=
POSTGRES_DB=techbrief
